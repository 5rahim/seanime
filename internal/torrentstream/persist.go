package torrentstream

import "github.com/anacrolix/torrent"

func (r *Repository) persistTorrent(torrent *torrent.Torrent) error {
	//// Get the torrent info
	//torrentInfo := r.getTorrentInfo(torrent)
	//
	//// Save the torrent to the database
	//err := r.db.SaveTorrent(torrentInfo)
	//if err != nil {
	//	return err
	//}
	//
	return nil
}
